spring:
  application:
    name: springconfclient
  profiles:
    active: dev, another
  cloud:
    config:
      uri: http://localhost:8888
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest