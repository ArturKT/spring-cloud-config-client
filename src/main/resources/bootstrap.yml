spring:
  application:
    name: config-client
#  profiles:
#    active: dev
  cloud:
    config:
      uri: http://localhost:8888
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest